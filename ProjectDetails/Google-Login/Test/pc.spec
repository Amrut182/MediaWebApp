# EmailID
		Validity:
				Exist.
				Non-Exist.		[error]
		Length:
				Empty.			[error]
				Non-Empty.
		Format:
				Proper Format(with@, .com).		[single]
				With(only @).		[error]
				With(@gmail).		[error]
				username.
				
# Password
		Length:
				Empty.				[error]
				Non-Empty.
		Validity:
				Valid.
				Invalid.				[error]
				
# Google Login Button:
		Presence:
				Yes.
				No.					[error]
		URL:
				Correct URL.
				Wrong URL.			[error]
				